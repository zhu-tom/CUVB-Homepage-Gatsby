import Section from "../components/section";
import SectionHeader from "../components/section-header";

import React from 'react';

export default function Details() {
    return (
        <Section>
            <SectionHeader titleText="Account Details"/>
        </Section>
    );
}